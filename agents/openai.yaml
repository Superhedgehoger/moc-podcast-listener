interface:
  display_name: "播客代听助手"
  short_description: "生成可追溯播客转录，归档图文并核验总结产物"
  default_prompt: "Use $moc-podcast-listener to prefer a publisher transcript when available, create traceable transcript artifacts, archive Show Notes, produce an evidence-backed summary, and verify the completed job."
